---
import navigationData from "../../data/navigation-data/navigationData";
const astro = Astro.url.pathname;
---

<menu
  class="hidden mobile-large:flex h-full justify-between items-center px-12 gap-9 font-barlow-condensed uppercase text-sm tracking-[2.36px] bg-white/5 backdrop-blur-[82px] tablet:pr-[106.5px] laptop:justify-end laptop:gap-8.25 laptop:text-base laptop:tracking-[2.7px] laptop:col-start-3 laptop:col-end-6 laptop:row-1 desktop:pr-41.25 desktop:pl-16 desktop:gap-3.75 desktop:justify-end desktop-xl-large:gap-7 desktop-xl-large:pl-12"
>
  {
    navigationData.map((navItem, i) => {
      const isActive = navItem.href === astro;
      return (
        <li class="relative h-full grid place-items-center">
          <a
            href={navItem.href}
            class="inline-flex justify-start items-center gap-2 desktop-xl-large:gap-2.75"
          >
            <span class="hidden desktop:block font-bold">{`0${i}`}</span>
            {navItem.name}
          </a>
          {isActive && <div class="absolute bottom-0 h-0.75 w-full bg-white" />}
        </li>
      );
    })
  }
</menu>
